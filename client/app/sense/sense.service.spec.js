'use strict';

describe('Service: sense', function () {

  // load the service's module
  beforeEach(module('html5editorApp'));

  // instantiate service
  var sense;
  beforeEach(inject(function (_sense_) {
    sense = _sense_;
  }));

  it('should do something', function () {
  });

});
